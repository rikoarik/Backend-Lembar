# Worklog — 2026-07-28

## BE — POST /v1/payment/orders default amountCents

**Task:** Make `POST /v1/payment/orders` succeed with `{toPlan:'pro'}` alone
(no `amountCents`). Demo flow was passing `amountCents=0`; route was rejecting
it as missing-required (400 VALIDATION_FAILED).

**Patch (minimum):** `src/modules/payment/adapters/http/webhookRoutes.ts`
- Split the missing-field check: only `toPlan` is now required.
- Default `amountCents` to `0` via `const finalAmountCents = amountCents ?? 0`.
- Wired `finalAmountCents` into `paymentService.createOrder`.

No service-side change — PaymentService already accepts any number for
`amountCents` and persists it. Adding a BFF-computed amount belongs at the
BFF/pricing layer, not here.

**Verify (curl, port 4000):**
- `{toPlan:"pro"}` → **201** with `amountCents:0`, `toPlan:"pro"`, `status:"pending"`.
- `{amountCents:0}` (no toPlan) → **400 VALIDATION_FAILED** "Body must include toPlan".
- Same `x-idempotency-key` retry → **200** with `idempotent:true` (existing order returned).

**Build:** `pnpm build` → exit 0 (pre-existing drizzle/pino TS noise unrelated).
**Deploy:** `pm2 restart lembar-api` → online on `127.0.0.1:4000`.
**Commit:** `0ddd909 fix(payment): default amountCents to 0 when omitted (demo/manual flow)`
**Push:** origin/dev `7922d63..0ddd909`.

**Skipped:** wiring real BFF pricing; rejecting `amountCents=0` once real
provider lands — `ponytail:` marker in code names the upgrade path.