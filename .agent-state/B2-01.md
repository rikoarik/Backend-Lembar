# B2-01 — Private PDF Upload Lifecycle

## Objective
Close B2-01 per .impeccable/contracts/B2-01.md + docs/adr/B2-01-private-pdf-upload.md.

## Current state (2026-07-20)
- Status in registry: IN_PROGRESS
- Task branch: feat/B2-01-private-pdf-upload-lifecycle (in worktree backend-lembar-27)
- Working commits already on branch:
  - 39b8236 wip: complete B2-01 implementation
  - dd8ed51 wip: preserve orphaned B2-01 upload partial
- Untracked deliverable copies also live in main-repo working tree (dev branch) but were the source of stash@{0} "B2-01-workarea" in the worktree before I dropped a duplicate wrong stash on dev. Branch state preserved.
- Stash @{0} on feat/B2-01-private-pdf-upload-lifecycle still holds B2-01-workarea; only src/bootstrap/app.ts had a conflict during an attempted pop. Markers removed by re-stashing.
- registry TASKS table still says latest_pushed_commit absent; branch has not been pushed beyond initial.

## Blocker awaiting owner decision
1. Are the wip commits 39b8236 + dd8ed51 acceptable as the authoritative B2-01 implementation, or must they be re-authored per the contract (ADR warns "the worker must re-author every file against .impeccable/contracts/B2-01.md")?
2. Resolve src/bootstrap/app.ts conflict — does the wip branch already register uploads routes? If yes, the workarea stash would double-register; if no, keep stash and resolve manually.
3. Confirm push target: per branch_policy.merge_flow, push to feat/B2-01-* and stop; orchestrator merges to ao/backend-integration. Do NOT auto-merge to dev.

## Files changed (from wip commits)
- drizzle.config.ts
- package.json
- src/bootstrap/app.ts (CONFLICT — needs owner resolution)
- src/infrastructure/database/migrations/0009_source_uploads.sql
- src/infrastructure/database/migrations/meta/{0008_snapshot.json,0009_snapshot.json,_journal.json}
- src/modules/uploads/{adapters/http/{preHandler.ts,routes.ts}, application/createUploadsService.ts, domain/{SourceUpload.ts,SourceUploadsService.ts}, persistence/{InMemorySourceUploadsStore.ts,PostgresSourceUploadsStore.ts,schema.ts}, policy/UploadPolicies.ts}
- src/smoke/upload.ts

## Verification still required
- pnpm install / typecheck / lint / format:check / build
- pnpm test (focus: test/modules/uploads/lifecycle.test.ts)
- pnpm openapi:validate && pnpm openapi:breaking (additive only)
- pnpm db:check
- DATABASE_URL=... pnpm upload:smoke (if script exists; else src/smoke/upload.ts via pnpm tsx)
- DATABASE_URL=... pnpm db:smoke

## Next exact action
Owner answers three blocker questions, then in worktree backend-lembar-27:
1. `git checkout --theirs src/bootstrap/app.ts` (or `--ours`) per owner direction, OR manually merge.
2. `pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint && pnpm format:check && pnpm build && pnpm test && pnpm openapi:validate && pnpm openapi:breaking && pnpm db:check`
3. `pnpm upload:smoke` and `pnpm db:smoke` against DATABASE_URL
4. Commit resolution, push branch, update TASK-REGISTRY.yaml (status=ACCEPTED pending owner, latest_pushed_commit, integration_commit=(pending)), open PR, end READY_FOR_OWNER_REVIEW.
